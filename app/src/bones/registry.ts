/**
 * Boneyard bones registry for the app/ package.
 *
 * Run `npx boneyard-js build` with the dev server running to generate
 * .bones.json files. Each skeleton name maps to a generated bone file
 * that captures the exact DOM layout of the real component.
 *
 * When bones are present, <Skeleton> shows auto-generated placeholders
 * during loading. Without bones, children render normally.
 */
export {};
